# docker-compose.yml (en la raíz del repo padre)
version: "3.9"

services:
  echo-shop-db:
    build:
      context: ./submodules/eco-shop-db
      dockerfile: Dockerfile
    container_name: echo-shop-db
    environment:
      POSTGRES_USER: echo_user
      POSTGRES_PASSWORD: echo_pass
      POSTGRES_DB: postgres
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --lc-collate=C --lc-ctype=C"
    ports:
      - "5432:5432"
    volumes:
      - eco_shop_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

volumes:
  eco_shop_db_data:
    driver: local
